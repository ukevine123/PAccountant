
@using Application.Interfaces
@using Application.Services
@using Application.DTO

@inject ICurrencyService CurrencyService

@inject NavigationManager Navigation
<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Inventory" Class="mr-3 mb-n1" />
            New Currency
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudForm @ref="form" @bind-IsValid="@success">
            <MudStack Spacing="4">
                            <MudTextField Label="Name" HelperText="Enter currency name"
                              @bind-Value="Model.Name" For="@(() => Model.Name)"/>

                            <MudTextField Label="Symbol" HelperText="Enter currency symbol"
                              @bind-Value="Model.Symbol" For="@(() => Model.Symbol)"/>
            </MudStack>
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Success" 
                   Variant="Variant.Filled" 
                   Disabled="@(!success)" 
                   OnClick="OnValidSubmit">Save</MudButton>
    </DialogActions>
</MudDialog>

@code { 

    [CascadingParameter]
    IMudDialogInstance MudDialogInstance { get; set; }
    
    // Form state
    bool success;
    MudForm form;
    private CurrencyCreateDTO  Model {get; set;} = new();

    private void OnValidSubmit()
    {
        CurrencyService.CreateCurrency(Model);
        
        Navigation.NavigateTo("/accounts");
    }  
    void Cancel() => MudDialogInstance.Cancel();

}
